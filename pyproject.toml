[project]
name = "aituber-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests",
    "python-dotenv",
    "obs-websocket-py"
]
  
[project.optional-dependencies]
dev = [
    "pytest>=6.0",
    "pytest-asyncio>=0.18"
]
